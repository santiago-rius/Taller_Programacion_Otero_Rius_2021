<!DOCTYPE html>

<div class="juego">
    <img src="img/{$jueg.imagen}" />
    <a href="juego.php?id=
    {$jueg.id}">
    <span class="nombre-producto">
    {$jueg.nombre} </span></a>
    <p> {$jueg.descripcion}</p>
    <span class="precio-producto"> U$S 
        {$jueg.precio} </span>
</div>
